this is a test to see if github is ok
it seems that if something wrong
i delete those files that cannot commit
perhaps dont have to delete those error files

issue command like
git status
git add --all
git commit -m "this is my first or N commit"
git push
